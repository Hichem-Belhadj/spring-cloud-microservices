spring:
  application:
    name: config-service
  cloud:
    discovery:
      enabled: true
    config:
      server:
        git:
          uri: https://github.com/Hichem-Belhadj/spring-cloud-microservices/config-git-repo
